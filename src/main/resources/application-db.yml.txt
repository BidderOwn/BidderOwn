spring:
  datasource:
    # mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://{IP}:{Port}/{DATABASE_NAME}?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Seoul

    # mariadb
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://{IP}:{Port}/{DATABASE_NAME}?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Seoul

    # mariadb url e.g.
    url: jdbc:mariadb://localhost:3306/bidder_own?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Seoul

    # e.g. root
    username: {USERNAME}
    password: {PASSWORD}

  redis:
    host: 127.0.0.1
    port: 6379

custom:
  redis:
    item:
      bidding:
        info-key: 'bidding-item:'
        updated-at-key: 'bidding-item:updated-at'
        comment-count-key: 'comment-count'
        bid-count-key: 'bid-count'